function y = func(x)
    
    %y = x; % linear
    
    %y = exp(x); % exponential
    
    %y = log(abs(x)); % logarithmic
    
    %y = x.^2; % power
    
    %y = mod(x, 2); % modulus
    
    y = sin(x); % trigonometric
    
end

